workspaces-experimental true

# React Native / Metro exclude Node 25 in their engine declarations.
# Keep this compatibility override scoped to this project's install command.
--install.ignore-engines true
